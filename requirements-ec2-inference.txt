# EC2 / Linux — Session 1 agent + NIM client (no PyTorch).
# Use on inference nodes where you only run Docker NIM + this Python stack.

pydantic>=2.0,<3
langchain-core>=0.3
langchain-openai>=0.2
langgraph>=0.2
python-dotenv>=1.0
httpx>=0.27
