# Sample inputs (Session 0–4)

Place golden examples here for the **log triage copilot**:

- `incident_*.json` — alert metadata + log excerpts (what you paste into the agent).

Later you can add:

- `eval_cases.jsonl` — one JSON object per line: `input` + expected checks.
